# SUGGESTION: generated by Claude — review before committing. Remove this line after review.

[project]
name = "enauto-v2"
version = "0.1.0"
description = "Cisco 300-435 ENAUTO v2.0 study repository"
requires-python = ">=3.10"

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "B", "UP"]
ignore = []

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py", "*_test.py"]
