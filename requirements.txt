# SUGGESTION: generated by Claude — review before committing. Remove this line after review.
#
# ENAUTO 300-435 v2.0 — Python dependencies
# Versions unpinned at first install. After `pip install -r requirements.txt` succeeds,
# freeze the working set with: pip freeze > requirements.lock

# --- Section 1.0: Foundation (YANG, NETCONF/RESTCONF) ---
ncclient                  # NETCONF client (1.2, 2.2)
requests                  # RESTCONF + all REST APIs (1.2, 2.3, 3.x)
pyang                     # YANG validation, tree generation (1.3, 1.5)
lxml                      # XML payload handling (1.4)

# --- Section 2.0: Device-Level ---
netmiko                   # CLI automation (2.1)
paramiko                  # SSH (netmiko dep, pinned explicitly for clarity)
jinja2                    # templates (3.3, also used device-level)

# --- Section 3.0: Controller-Based ---
meraki                    # Meraki official SDK (3.2 Meraki)
catalystcentersdk         # Catalyst Center SDK (3.x CatC). If pip can't resolve, try `dnacentersdk` (legacy name).
ciscoisesdk               # ISE community SDK (3.5)
# Catalyst SD-WAN: no official SDK — using `requests` directly.

# --- Section 3.4: Ansible ---
ansible-core              # engine only; install collections separately via:
                          # ansible-galaxy collection install cisco.ios cisco.dnac cisco.meraki cisco.ise

# --- Section 5.0: AI in Automation ---
fastmcp                   # MCP server build (5.4, Week 11)

# --- Common ---
python-dotenv             # .env loading
